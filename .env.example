DATABASE_URL=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Comma-separated list of emails allowed into /admin
ADMIN_EMAILS=
# UPI VPA that receives payments via the "Pay via UPI" intent link/QR on orders.
# No gateway involved — payment confirmation is still manual via the admin panel's
# "Mark as paid" toggle, since a personal/business VPA has no webhook API.
UPI_VPA=
UPI_PAYEE_NAME=
